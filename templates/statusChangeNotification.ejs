<!DOCTYPE html>
<html>
<head>
    <style>
        body { font-family: Arial, sans-serif; line-height: 1.6; }
        .container { max-width: 600px; margin: 0 auto; padding: 20px; }
        .status { font-weight: bold; }
        .footer { margin-top: 20px; font-size: 12px; color: #6b7280; }
    </style>
</head>
<body>
    <div class="container">
        <%
            var statusColor = (newStatus === 'activated') ? '#16a34a' : '#dc2626';
        %>
        <p>Hello <%= userName %>,</p>
        <p>Your account has been <span class="status" style="color:white"><%= newStatus %></span> by an administrator.</p>
        <% if (newStatus === 'deactivated') { %>
            <p>You will no longer be able to access your account until it is reactivated.</p>
        <% } %>
        <p>If you believe this was done in error, please contact us at <a href="mailto:<%= contactEmail %>"><%= contactEmail %></a>.</p>
        <div class="footer">
            <p>This is an automated notification. Please do not reply to this email.</p>
        </div>
    </div>
</body>
</html>